// 패럴렉스 PJ JS - main.js
import startSS from "./smoothScroll20.js";
startSS()
// parallax=when scroll, same direction but different speed for each element
// need the setting of range
// use .getBoundingClientRect().top
// also use scroll event